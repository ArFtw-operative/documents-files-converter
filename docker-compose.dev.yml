services:
  api:
    environment: {APP_ENV: development}
    ports: ['8000:8000']
    volumes: ['./apps/api:/app']
    command: [uvicorn, 'convertvault.main:app', --host, 0.0.0.0, --port, '8000', --reload]
  web:
    ports: ['3000:3000']
  minio:
    ports: ['9000:9000', '9001:9001']

